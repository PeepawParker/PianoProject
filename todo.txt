Tomorrow TODO:

    Add a test tab where you can go through each note on your keyboard and confirm that the note is mapped to the correct key to make it easy for users to see what keys should be remapped

    Have key tester where users can see which keys are out of tune and need to be remapped

Make the number of notes you practice dynamic with users picking the number of notes they want to be displayed up to an extent

The new random notes aren't always within the selected range figure out why this is because it shouldn't be happening. Might not even being the random number generator maybe the array just comes back out of order so its pulling a random high note

Low note values aren't being collected at allright now, need to tweak the logic for collecting the low notes I think they are getting filtered out right now

Allow user to select either treble/bass clef or both to practice on

Metronome

Have a ear test where it plays a note and has the user play back the corresponding note

Don't move onto next note if no data was collected when remapping the notes

Have an up/down key when remapping keys instead of the dumb retry button that doesn't work

Fix error when updating the final note don't let it increment again where it's out of range

Seperate the PracticePage and the practice start page so not all of the code is on a single page

Make the website look nicer

Review code and clean it up